bin/
tmp/
dist/
replay-viewer/dist/nimcache/
*.replay
# The committed fixtures (sixmax_audit, chiprace_bust) are regenerated and diffed by tests/test_sim.nim.
!tools/ci/fixtures/*.replay
# Host-specific nimby package paths; regenerate per machine (see README).
nim.cfg
